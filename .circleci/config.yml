---
version: 2
jobs:
  build:
    working_directory: ~/dropwizard
    environment:
      JAVA_OPTS: -Xms512m -Xmx2g -XX:+TieredCompilation -XX:TieredStopAtLevel=1
    docker:
      # yamllint disable-line rule:line-length
      - image: cimg/openjdk:25.0.4@sha256:c2fae0b91d4c591f200f1d0221234a994a13436bc1f90dabb37902dd66504ee2
    steps:
      - checkout
      - restore_cache:
          key: dropwizard-{{ checksum "pom.xml" }}
      - run: ./mvnw --no-transfer-progress -V -B -ff
          -Dsurefire.rerunFailingTestsCount=1 install
      - save_cache:
          paths:
            - ~/.m2
          key: dropwizard-{{ checksum "pom.xml" }}
      - run:
          name: Save test results
          command: |
            mkdir -p ~/junit/
            mv ~/dropwizard/*/target/surefire-reports/*.xml ~/junit/
          when: always
      - store_test_results:
          path: ~/junit
      - store_artifacts:
          path: ~/junit
