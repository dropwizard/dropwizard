server:
    applicationConnectors:
        - type: https
          port: 0
          keyStorePath: keystore.jks
          keyStorePassword: password
    adminConnectors:
        - type: https
          port: 0
          keyStorePath: keystore.jks
          keyStorePassword: password
